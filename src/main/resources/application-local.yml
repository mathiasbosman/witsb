spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: witsb_dba
    password: admin
    url: jdbc:postgresql://localhost:7432/witsb_db
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.hibernate.type.descriptor.sql: trace
    be.mathiasbosman.witsb: trace
server:
  port: 8081